# Recursively collect file paths from a volume directory

Recursively collect file paths from a volume directory

## Usage

``` r
db_volume_list_files_recursive(path, recurse = TRUE, host, token)
```
