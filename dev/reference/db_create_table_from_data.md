# Create table from data frame structure

Create table from data frame structure

## Usage

``` r
db_create_table_from_data(
  conn,
  quoted_name,
  value,
  field.types,
  temporary = FALSE,
  overwrite = FALSE
)
```
