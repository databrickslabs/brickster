# Test if object is of class MavenLibrary

Test if object is of class MavenLibrary

## Usage

``` r
is.lib_maven(x)
```

## Arguments

- x:

  An object

## Value

`TRUE` if the object inherits from the `MavenLibrary` class.
